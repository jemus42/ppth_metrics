# Tautulli Configuration
# URL of your Tautulli instance
TAUTULLI_URL=http://tautulli.local:8181
# API key from Tautulli settings (Settings -> Web Interface -> API -> API key)
TAUTULLI_API_KEY=your_api_key_here

# Exporter Configuration
# Port to expose the metrics endpoint
EXPORTER_PORT=8000
# Bind address for the exporter (0.0.0.0 for all interfaces)
EXPORTER_BIND_ADDRESS=0.0.0.0

# Version Configuration
# Docker image version to use (defaults to latest, can be pinned to specific version)
# VERSION=0.1.0